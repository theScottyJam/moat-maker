// You can switch this to true to disable all parameter validation to the public API.
// When debugging, this can help remove noise of unrelated logic executing, and potentially failing.
export const DISABLE_PARAM_VALIDATION = true;
